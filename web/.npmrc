# NPM configuration for CI/CD pipeline
# Disable optional dependencies to avoid platform-specific issues
optional=false

# Use npm's legacy peer dependency resolution
legacy-peer-deps=true

# Ensure consistent installs
package-lock=true

# Disable audit during CI (can be slow)
audit=false

# Use npm's built-in registry
registry=https://registry.npmjs.org/
